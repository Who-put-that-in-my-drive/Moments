export interface Image {
    title: string,
    format: string,
    size: string,
    caption: string,
    tags: string[],
    categories: string[],
    owner: string,
    location: string,
    lastModifiedDateTime: number,
    uploadedDateTime: number,
}
